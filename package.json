{
  "name": "drivelegal-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "build:frontend": "npm run build --prefix frontend",
    "seed:backend": "npm run seed --prefix backend"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
